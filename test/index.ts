import { expect } from "chai";
import { ethers } from "hardhat";

const { loadFixture } = require("@nomicfoundation/hardhat-network-helpers");
//describe("UsageBasedInsurance", function () {});
// for chai - npm install --save-dev @nomicfoundation/hardhat-chai-matchers 
// for hardhat - npm install --save-dev @nomicfoundation/hardhat-chai-matchers chai @nomicfoundation/hardhat-ethers ethers.